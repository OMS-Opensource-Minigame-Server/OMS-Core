name: Build and deploy

on:
  push:
    branches:
      - main

jobs:
  maven:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v1
        with:
          fetch-depth: 1

      - uses: actions/setup-java@v2
        with:
          java-version: '16'
          distribution: 'adopt'

      - name: Publish package
        run: mvn --batch-mode deploy
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}



  deploy-spigot:
    runs-on: ubuntu-latest

    needs: [maven]

    steps:
      - name: Checkout
        uses: actions/checkout@v1
        with:
          fetch-depth: 1

      - name: Log in to GitHub Docker Registry
        uses: docker/login-action@v1
        with:
          registry: docker.pkg.github.com
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build container image
        uses: docker/build-push-action@v2
        with:
          context: ./spigot-core
          file: ./spigot-core/Dockerfile
          push: true
          tags: |
            docker.pkg.github.com/oms-opensource-minigame-server/oms-core/oms-core:${{ github.sha }}
            docker.pkg.github.com/oms-opensource-minigame-server/oms-core/oms-core:latest
  docker-bungee:
    runs-on: ubuntu-latest

    needs: [maven]

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v1
        with:
          fetch-depth: 1

      - name: Log in to GitHub Docker Registry
        uses: docker/login-action@v1
        with:
          registry: docker.pkg.github.com
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build container image
        uses: docker/build-push-action@v2
        with:
          context: ./bungee-core
          file: ./bungee-core/Dockerfile
          push: true
          tags: |
            docker.pkg.github.com/oms-opensource-minigame-server/oms-bungee/oms-bungee:${{ github.sha }}
            docker.pkg.github.com/oms-opensource-minigame-server/oms-bungee/oms-bungee:latest